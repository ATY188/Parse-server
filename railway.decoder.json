{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.decoder"
  },
  "deploy": {
    "startCommand": "sh -c 'uvicorn decoder-server:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}

